# task-automation
Automate moving .jpg images from a source folder to a destination folder using Python's os and shutil modules. The script automatically creates the destination directory if it doesn't exist, filters for JPEG files, transfers them efficiently, and provides real-time console updates on the transfer progress.
